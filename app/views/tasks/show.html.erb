<% content_for :title, (@task.subject.presence || "Detalhes da tarefa") %>

<div class="space-y-8">
  <!-- Header -->
  <div class="flex flex-col gap-6">
    <div class="flex flex-col lg:flex-row items-start lg:items-center justify-between gap-6">
      <div class="min-w-0 flex-1">
        <p class="text-sm font-medium text-gray-500 dark:text-neutral-400 mb-2">Detalhes da tarefa</p>
        <h1 class="text-3xl lg:text-4xl font-bold text-gray-900 dark:text-neutral-100 leading-tight">
          <%= @task.subject.presence || "Sem título" %>
        </h1>

        <p class="mt-3 text-sm text-gray-600 dark:text-neutral-400">
          Do onboarding <span class="font-medium text-gray-900 dark:text-neutral-200"><%= link_to @task.onboarding.try(:title).presence || 'Sem título', onboarding_path(@task.onboarding), class: "hover:underline text-blue-600 dark:text-blue-400" %></span>
        </p>

        <div class="mt-4 flex flex-wrap items-center gap-3">
          <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold <%= status_badge_class(@task.status) %>">
            <%= status_label(@task.status) %>
          </span>

          <% if @task.priority.present? %>
            <span class="inline-flex items-center px-2.5 py-1 rounded text-xs font-medium bg-amber-100 text-amber-800 dark:bg-amber-900/30 dark:text-amber-400">
              <%= @task.priority %>
            </span>
          <% end %>

          <% if @task.assignee_id.present? && (assignee_user = User.find_by(id: @task.assignee_id)) %>
            <div class="flex items-center gap-2 pl-2 border-l border-gray-300 dark:border-neutral-600">
              <%= image_tag avatar_for(assignee_user), class: "w-6 h-6 rounded-full" %>
              <span class="text-xs text-gray-600 dark:text-neutral-400"><%= assignee_user.full_name rescue assignee_user.email %></span>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Actions -->
      <div class="flex flex-col sm:flex-row items-stretch sm:items-center gap-2 sm:gap-3">
        <% if @task.status != 'completed' %>
          <button type="button" data-action="click"
                  class="inline-flex items-center justify-center gap-2 px-4 py-2.5 rounded-lg bg-green-600 hover:bg-green-700 text-white text-sm font-semibold shadow transition dark:bg-green-700 dark:hover:bg-green-600">
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
            </svg>
            Concluir
          </button>
        <% end %>

        <%= link_to "Editar", edit_onboarding_task_path(@task.onboarding, @task),
            class: "inline-flex items-center justify-center px-4 py-2.5 rounded-lg border border-gray-300 text-gray-700 text-sm font-medium hover:bg-gray-50 shadow-sm transition dark:border-neutral-600 dark:text-neutral-300 dark:hover:bg-neutral-700" %>

        <%= link_to "← Voltar", onboarding_path(@task.onboarding), 
            class: "inline-flex items-center justify-center px-4 py-2.5 text-gray-600 text-sm font-medium hover:text-gray-900 transition dark:text-neutral-400 dark:hover:text-neutral-200" %>
      </div>
    </div>
  </div>

    <!-- MAIN GRID: left = descrição/anexos/comentários (2fr), right = meta (1fr) -->
    <div class="mt-10 grid grid-cols-1 lg:grid-cols-[2fr_1fr] gap-8">

      <!-- LEFT: description / attachments / comments -->
      <main class="space-y-6">

        <!-- DESCRIPTION box (separada, ampla) -->
        <section class="bg-white rounded-xl shadow-md ring-1 ring-slate-100 p-6">
          <h2 class="text-lg font-semibold text-slate-800 mb-4">Descrição</h2>

          <div class="prose max-w-none text-slate-700">
            <% if @task.body.present? %>
              <%= simple_format(@task.body) %>
            <% else %>
              <div class="text-sm text-slate-400 bg-slate-50 p-4 rounded-md">Nenhuma descrição fornecida.</div>
            <% end %>
          </div>

          <div class="mt-6 grid grid-cols-1 sm:grid-cols-2 gap-4 text-sm text-slate-600">
            <div>
              <p class="text-xs text-slate-400">Lembrete</p>
              <p class="mt-1"><%= @task.reminder.present? ? l(@task.reminder, format: :short) : '—' %></p>
            </div>

            <div>
              <p class="text-xs text-slate-400">HubSpot ID</p>
              <p class="mt-1 text-slate-700"><%= @task.hubspot_id.presence || '—' %></p>
            </div>
          </div>
        </section>

        <!-- ATTACHMENTS box -->
        <section class="bg-white rounded-xl shadow-md ring-1 ring-slate-100 p-6">
          <h3 class="text-md font-semibold text-slate-800 mb-3">Anexos</h3>
          <div class="border-2 border-dashed border-slate-100 rounded-md p-6 text-sm text-slate-500 bg-slate-50">
            <p>Ainda não há sistema de arquivos — espaço reservado para anexos.</p>
            <div class="mt-4">
              <button class="px-3 py-1.5 rounded-md bg-white text-sm hover:bg-slate-50 border">Adicionar anexo</button>
            </div>
          </div>
        </section>

        <!-- COMMENTS box -->
        <section class="bg-white rounded-xl shadow-md ring-1 ring-slate-100 p-6">
          <h3 class="text-md font-semibold text-slate-800 mb-3">Comentários</h3>
          <div class="border-2 border-dashed border-slate-100 rounded-md p-6 text-sm text-slate-500 bg-slate-50">
            <p>Ainda não há a opção de comentários.</p>
          </div>
        </section>

      </main>

      <!-- RIGHT: compact meta column -->
      <aside class="space-y-6">

        <!-- DATES & STATUS box -->
        <div class="bg-white rounded-xl shadow-md ring-1 ring-slate-100 p-6">
          <h3 class="text-md font-semibold text-slate-800 mb-4">Datas & status</h3>

          <dl class="text-sm text-slate-700 space-y-3">
            <div class="flex justify-between">
              <dt class="text-xs text-slate-400">Vencimento</dt>
              <dd class="font-medium"><%= @task.due_date.present? ? @task.due_date.strftime("%d/%m/%Y %H:%M") : '—' %></dd>
            </div>

            <div class="flex justify-between">
              <dt class="text-xs text-slate-400">Conclusão</dt>
              <dd class="font-medium"><%= @task.completion_date.present? ? @task.completion_date.strftime("%d/%m/%Y %H:%M") : '—' %></dd>
            </div>

            <div class="flex justify-between">
              <dt class="text-xs text-slate-400">Prioridade</dt>
              <dd class="font-medium"><%= @task.priority.presence || '—' %></dd>
            </div>

            <div class="flex justify-between">
              <dt class="text-xs text-slate-400">Visibilidade</dt>
              <dd class="font-medium"><%= @task.visibility ? 'Visível' : 'Oculta' %></dd>
            </div>

            <div class="flex justify-between">
              <dt class="text-xs text-slate-400">Criado em</dt>
              <dd class="font-medium"><%= l(@task.created_at, format: :short) %></dd>
            </div>
          </dl>
        </div>

        <!-- ASSIGNEE / quick contact -->
        <div class="bg-white rounded-xl shadow-md ring-1 ring-slate-100 p-6">
          <h3 class="text-md font-semibold text-slate-800">Responsável</h3>

          <% if @task.assignee_id.present? && (assignee_user = User.find_by(id: @task.assignee_id)) %>
            <div class="mt-3 flex items-center gap-3">
              <%= image_tag avatar_for(assignee_user), class: "w-12 h-12 rounded-full" %>
              <div class="min-w-0">
                <p class="text-sm font-medium text-slate-800"><%= assignee_user.full_name rescue assignee_user.email %></p>
                <p class="text-xs text-slate-500"><%= assignee_user.email %></p>
                <% if assignee_user.respond_to?(:phone) && assignee_user.phone.present? %>
                  <p class="text-xs text-slate-500 mt-1">📞 <%= assignee_user.phone %></p>
                <% end %>
              </div>
            </div>
          <% else %>
            <p class="text-sm text-slate-500 mt-2">Sem responsável definido.</p>
          <% end %>
        </div>

      </aside>
    </div>
  </div>
</div>
